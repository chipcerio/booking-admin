import React, { PureComponent } from 'react';
import { Text, View } from 'react-native';

class BookingsScreen extends PureComponent {
  render() {
    return (
      <View>
        <Text> BookingsScreen </Text>
      </View>
    );
  }
}

export default BookingsScreen;
